apiVersion: apps/v1
kind: Deployment # Kubernetes resource kind we are creating
metadata:
  name: ems-rct-app-dep
spec:
  selector:
    matchLabels:
      app: ems-rct-app
  replicas: 2 # Number of replicas that will be created for this deployment
  template:
    metadata:
      labels:
        app: ems-rct-app
    spec:
      containers:
        - name: ems-rct-app
          image: raj939098/ems-rct-app:latest # Image that will be used to containers in the cluster
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5173 # The port that the container is running on in the cluster